{% extends 'base.html.twig' %}

{% block title %}UserGroup{% endblock %}

{% block body %}
    <h1>UserGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_group.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ user_group.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_usergroup_index') }}">back to list</a>

    <a href="{{ path('app_usergroup_edit', {'id': user_group.id}) }}">edit</a>

    {{ include('usergroup/_delete_form.html.twig') }}
{% endblock %}
